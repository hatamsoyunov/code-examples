export enum TransactionStatus {
  IDLE = 'idle',
  SUCCESS = 'success',
  FAILED = 'failed',
}

export enum TransactionType {
  SERVICE_SUBSCRIBE = 'serviceSubscribe',
  COURSE_PURCHASE = 'coursePurchase',
  MONTHLY_SUBSCRIBE = 'monthlySubscribe',
  BOOK_PURCHASE = 'bookPurchase',
}
